name: todolist-dev

services:
  db:
    image: "postgres:18"
    ports: 
      - 5432:5432
    environment:
      POSTGRES_USER: "postgres"
      POSTGRES_PASSWORD: "postgres"
      POSTGRES_DB: "todolist"

networks:
  todolist-network:
    driver: bridge
